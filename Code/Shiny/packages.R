if (!require("armtrack"))
{
  install.packages("c:/TFM/Code/Shiny/armtrack_0.0.0.9000.tar.gz", repos = NULL, type = "source")
  library(armtrack)
}
if (!require("gganimate"))
{
  install.packages("gganimate")
  library(gganimate)
}
if (!require("ggplot2"))
{
  install.packages("ggplot2")
  library(ggplot2)
}
if (!require("rgl"))
{
  install.packages("rgl")
  library(rgl)
}
if (!require("gg3D"))
{
  install.packages("c:/TFM/Code/Shiny/gg3D_0.0.0.9000.tar.gz", repos = NULL, type = "source")
  library(gg3D)
}
if (!require("matrixStats"))
{
  install.packages("matrixStats")
  library(matrixStats)
}
if (!require("rmatio"))
{
  install.packages("rmatio")
  llibrary(rmatio)
}
